## Packages
framer-motion | Page transitions, scroll animations, and counting stats
lucide-react | Icons for the UI (already in base, but ensuring it's used)
react-hook-form | Form handling for inquiries
zod | Schema validation
@hookform/resolvers | Zod resolver for react-hook-form

## Notes
Images are located in /images/ directory (copied from attached assets).
API routes are defined in @shared/routes.
Using "DM Sans" for headings and "Inter" for body text to give a clean, industrial look.
Primary color is Industrial Orange (#F57C00), Secondary is Navy Blue (#003366).
